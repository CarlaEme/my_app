name: my_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0  # <--- Se agregó esta librería necesaria para la API
  firebase_core: ^2.24.2   # <--- Agrega esta
  cloud_firestore: ^4.14.0 # <--- Agrega esta para subir datos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/imagen_principal.jpeg # <--- Espacios corregidos para evitar errores